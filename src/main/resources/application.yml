spring:
    h2:
        console:
            enabled: true
            path: /h2-console
    datasource:
        url: jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE
        driver-class-name: org.h2.Driver
        username: admin
        password:
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    batch:
        job:
            enabled: false
server:
    port: 8080